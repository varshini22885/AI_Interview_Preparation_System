# Frontend environment configuration (copy to .env.local for development)
# Base URL of the backend API. Development uses the Vite proxy configured
# in vite.config.js ("/api" -> http://localhost:8000), so the relative
# path below works without any host baked into application code.
VITE_API_BASE_URL=/api/v1
